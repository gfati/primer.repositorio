let giftCard = prompt ("Elige la GiftCard que quieras regalar");
alert ("El usuario ingreso " + giftCard);
let precioGiftUno = parseInt(alert("El valor es de $1600.-")); 
    {
    prompt ("Quieres abonar en 3 o 6 pagos?");
    }
    {
        if (cuotasUsuario=6);
        const dividirSeis  = function (a, b) {return 1600 / 6}; 
        alert (`Seran seis pagos de ${dividirSeis()}`);
    }
